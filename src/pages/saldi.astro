---
// Pagina Saldi Dedicata con integrazione CMS
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';

// Leggi i dati dal CMS (quando disponibili) o usa dati di fallback
let saldiPosts = [];
let prodottiSaldi = [];
let categorieSaldi = [];

try {
  // Prova a leggere dal CMS
  saldiPosts = await getCollection('saldi-posts');
  prodottiSaldi = await getCollection('prodotti-saldi');
  categorieSaldi = await getCollection('categorie-saldi');
} catch (error) {
  console.log('CMS non ancora configurato, uso dati di esempio');
}

// Fallback data se il CMS non è ancora configurato
const saldiHeroFallback = {
  titolo: "🔥 Saldi Straordinari",
  sottotitolo: "Scopri le nostre offerte imperdibili",
  descrizione: "Eleganza e stile con sconti fino al 70%. Approfitta delle nostre promozioni esclusive su abiti da cerimonia e sportswear di qualità.",
  immagineBackground: "/background.png",
  scontoMax: "70%",
  attivo: true
};

const categorieFallback = [
  {
    data: {
      nome: "Abiti da Cerimonia",
      sconto: "50%",
      descrizione: "Eleganza per ogni occasione speciale",
      immagine: "/abiti-cerimonia-eleganti.png",
      numeroArticoli: 15,
      attivo: true,
      ordine: 1
    }
  },
  {
    data: {
      nome: "Sportswear",
      sconto: "30%",
      descrizione: "Comfort e stile per il tempo libero",
      immagine: "/abbigliamento-sportivo.png",
      numeroArticoli: 22,
      attivo: true,
      ordine: 2
    }
  },
  {
    data: {
      nome: "Accessori",
      sconto: "40%",
      descrizione: "Completa il tuo look con stile",
      immagine: "/servizio-sartoriale.jpeg",
      numeroArticoli: 8,
      attivo: true,
      ordine: 3
    }
  }
];

const prodottiFallback = [
  {
    data: {
      nome: "Completo Elegante Blu",
      prezzoOriginale: 450,
      prezzoScontato: 270,
      descrizione: "Completo da cerimonia in tessuto pregiato, perfetto per matrimoni ed eventi importanti.",
      immagine: "/abiti-cerimonia-1.png",
      categoria: "Abiti da Cerimonia",
      taglie: ["48", "50", "52", "54"],
      disponibile: true,
      nuovo: false,
      limitedTime: true,
      ordine: 1
    }
  },
  {
    data: {
      nome: "Abito da Sera Nero",
      prezzoOriginale: 380,
      prezzoScontato: 190,
      descrizione: "Abito da sera elegante, ideale per cene di gala e eventi serali.",
      immagine: "/abiti-cerimonia-2.png",
      categoria: "Abiti da Cerimonia",
      taglie: ["46", "48", "50"],
      disponibile: true,
      nuovo: true,
      limitedTime: false,
      ordine: 2
    }
  },
  {
    data: {
      nome: "Tuta Sportiva Premium",
      prezzoOriginale: 120,
      prezzoScontato: 84,
      descrizione: "Tuta sportiva in tessuto tecnico traspirante, perfetta per sport e tempo libero.",
      immagine: "/sportswear-casual.png",
      categoria: "Sportswear",
      taglie: ["S", "M", "L", "XL"],
      disponibile: true,
      nuovo: false,
      limitedTime: false,
      ordine: 3
    }
  }
];

// Usa i dati del CMS se disponibili, altrimenti usa i fallback
const saldiHero = saldiPosts.length > 0 
  ? saldiPosts.find(post => post.data.attivo)?.data || saldiHeroFallback
  : saldiHeroFallback;

const categorieAttive = categorieSaldi.length > 0 
  ? categorieSaldi
      .filter(cat => cat.data.attivo)
      .sort((a, b) => (a.data.ordine || 0) - (b.data.ordine || 0))
  : categorieFallback;

const prodottiDisponibili = prodottiSaldi.length > 0
  ? prodottiSaldi
      .filter(prod => prod.data.disponibile)
      .sort((a, b) => (a.data.ordine || 0) - (b.data.ordine || 0))
  : prodottiFallback;

// Calcola le statistiche
const numeroTotaleProdotti = prodottiDisponibili.length;
const scontoMassimoCalcolato = prodottiDisponibili.reduce((max, prod) => {
  const sconto = Math.round(((prod.data.prezzoOriginale - prod.data.prezzoScontato) / prod.data.prezzoOriginale) * 100);
  return Math.max(max, sconto);
}, 0);
---
---

<Layout title="Saldi - Kelua | Offerte Speciali su Abbigliamento di Qualità">
  <main>
    <Navbar />
      <!-- Hero Section Saldi -->
    <section class="saldi-hero">
      <div class="hero-background">
        <img src={saldiHero.immagineBackground || "/background.png"} alt="Saldi Kelua" class="hero-bg-image">
        <div class="hero-overlay"></div>
      </div>
      
      <div class="hero-content">
        <div class="container">
          <div class="hero-text">
            <h1>{saldiHero.titolo}</h1>
            <h2>{saldiHero.sottotitolo}</h2>
            <p>{saldiHero.descrizione}</p>
            <div class="hero-stats">
              <div class="stat">
                <span class="number">{numeroTotaleProdotti}</span>
                <span class="label">Prodotti in Saldo</span>
              </div>
              <div class="stat">
                <span class="number">{scontoMassimoCalcolato || saldiHero.scontoMax}%</span>
                <span class="label">Sconto Massimo</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Categorie in Saldo -->
    <section class="categorie-saldi">
      <div class="container">
        <h2>Categorie in Promozione</h2>
        <div class="categorie-grid">
          {categorieAttive.map((categoria, index) => {
            const cat = categoria.data || categoria;
            return (
              <div class="categoria-card scroll-animate" data-delay={index * 0.2}>
                <div class="categoria-image">
                  <img src={cat.immagine} alt={cat.nome} />
                  <div class="sconto-overlay">
                    <span>-{cat.sconto}</span>
                  </div>
                </div>
                <div class="categoria-content">
                  <h3>{cat.nome}</h3>
                  <p>{cat.descrizione}</p>
                  <div class="categoria-meta">
                    <span class="articoli">{cat.numeroArticoli} articoli</span>
                    <span class="sconto-text">Fino a -{cat.sconto}</span>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </section>

    <!-- Prodotti in Saldo -->
    <section class="prodotti-saldi">
      <div class="container">
        <h2>Prodotti in Offerta</h2>
        <div class="prodotti-grid">
          {prodottiDisponibili.map((prodotto, index) => {
            const prod = prodotto.data || prodotto;
            const scontoCalcolato = Math.round(((prod.prezzoOriginale - prod.prezzoScontato) / prod.prezzoOriginale) * 100);
            return (
              <div class="prodotto-card scroll-animate" data-delay={index * 0.1}>
                <div class="prodotto-image">
                  <img src={prod.immagine} alt={prod.nome} />
                  <div class="badges">
                    {prod.nuovo && <span class="badge nuovo">Nuovo</span>}
                    {prod.limitedTime && <span class="badge limited">Tempo Limitato</span>}
                    <span class="badge sconto">-{scontoCalcolato}%</span>
                  </div>
                </div>
                
                <div class="prodotto-content">
                  <div class="prodotto-header">
                    <h3>{prod.nome}</h3>
                    <span class="categoria-tag">{prod.categoria}</span>
                  </div>
                  
                  <p class="descrizione">{prod.descrizione}</p>
                  
                  <div class="prezzi">
                    <span class="prezzo-originale">€{prod.prezzoOriginale}</span>
                    <span class="prezzo-scontato">€{prod.prezzoScontato}</span>
                    <span class="risparmio">Risparmi €{prod.prezzoOriginale - prod.prezzoScontato}</span>
                  </div>
                  
                  <div class="taglie">
                    <span class="taglie-label">Taglie disponibili:</span>
                    <div class="taglie-list">
                      {prod.taglie?.map(taglia => (
                        <span class="taglia">{taglia}</span>
                      ))}
                    </div>
                  </div>
                  
                  <div class="prodotto-actions">
                    <a href="#contatti" class="btn-acquista">
                      <span>Acquista Ora</span>
                      <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-1.5 1.5M6 16.5a1.5 1.5 0 113 0 1.5 1.5 0 01-3 0zM17 16.5a1.5 1.5 0 113 0 1.5 1.5 0 01-3 0z"/>
                      </svg>
                    </a>
                    <a href="tel:+390882452911" class="btn-info">
                      <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                      </svg>
                      <span>Chiama per Info</span>
                    </a>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </section>

    <!-- Call to Action -->
    <section class="cta-saldi">
      <div class="container">
        <div class="cta-content">
          <h2>Non Perdere le Nostre Offerte!</h2>
          <p>Vieni a trovarci in negozio per scoprire tutti i prodotti in saldo o contattaci per maggiori informazioni.</p>
          <div class="cta-buttons">
            <a href="#contatti" class="btn-primary">Vieni in Negozio</a>
            <a href="tel:+390882452911" class="btn-secondary">Chiama Subito</a>
          </div>
        </div>
      </div>
    </section>

    <Footer />
  </main>
</Layout>

<style>
  /* Hero Section Saldi */
  .saldi-hero {
    min-height: 70vh;
    position: relative;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
  }

  .hero-bg-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.6);
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(26, 26, 26, 0.8) 0%, rgba(0, 0, 0, 0.6) 100%);
    z-index: -1;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    width: 100%;
    padding: 120px 0 60px;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .hero-text {
    text-align: center;
    color: white;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero-text h1 {
    font-size: 4rem;
    font-weight: 700;
    margin-bottom: 16px;
    background: linear-gradient(45deg, #d4af37, #f1c40f);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-text h2 {
    font-size: 1.8rem;
    font-weight: 400;
    color: #f1c40f;
    margin-bottom: 20px;
  }

  .hero-text p {
    font-size: 1.2rem;
    color: #ccc;
    line-height: 1.6;
    margin-bottom: 40px;
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    margin-top: 40px;
  }

  .stat {
    text-align: center;
  }

  .stat .number {
    display: block;
    font-size: 3rem;
    font-weight: 700;
    color: #d4af37;
    line-height: 1;
  }

  .stat .label {
    display: block;
    font-size: 1rem;
    color: #ccc;
    margin-top: 8px;
  }

  /* Categorie Saldi */
  .categorie-saldi {
    padding: 100px 0;
    background: #f8f9fa;
  }

  .categorie-saldi h2 {
    font-size: 2.5rem;
    font-weight: 600;
    text-align: center;
    margin-bottom: 60px;
    color: #1a1a1a;
  }

  .categorie-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 40px;
  }

  .categoria-card {
    background: white;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
  }

  .categoria-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .categoria-image {
    position: relative;
    height: 200px;
    overflow: hidden;
  }

  .categoria-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .categoria-card:hover .categoria-image img {
    transform: scale(1.05);
  }

  .sconto-overlay {
    position: absolute;
    top: 20px;
    right: 20px;
    background: linear-gradient(45deg, #e74c3c, #c0392b);
    color: white;
    padding: 10px 20px;
    border-radius: 25px;
    font-weight: 700;
    font-size: 1.1rem;
  }

  .categoria-content {
    padding: 30px;
  }

  .categoria-content h3 {
    font-size: 1.5rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 12px;
  }

  .categoria-content p {
    color: #666;
    line-height: 1.6;
    margin-bottom: 20px;
  }

  .categoria-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .articoli {
    color: #666;
    font-size: 0.9rem;
  }

  .sconto-text {
    color: #e74c3c;
    font-weight: 600;
    font-size: 1rem;
  }

  /* Prodotti Saldi */
  .prodotti-saldi {
    padding: 100px 0;
    background: #1a1a1a;
  }

  .prodotti-saldi h2 {
    font-size: 2.5rem;
    font-weight: 600;
    text-align: center;
    margin-bottom: 60px;
    color: white;
  }

  .prodotti-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 40px;
  }

  .prodotto-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(212, 175, 55, 0.2);
    border-radius: 20px;
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .prodotto-card:hover {
    transform: translateY(-5px);
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(212, 175, 55, 0.4);
  }

  .prodotto-image {
    position: relative;
    height: 300px;
    overflow: hidden;
  }

  .prodotto-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .badges {
    position: absolute;
    top: 15px;
    left: 15px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .badge {
    padding: 6px 12px;
    border-radius: 15px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
  }

  .badge.nuovo {
    background: linear-gradient(45deg, #27ae60, #2ecc71);
    color: white;
  }

  .badge.limited {
    background: linear-gradient(45deg, #f39c12, #e67e22);
    color: white;
  }

  .badge.sconto {
    background: linear-gradient(45deg, #e74c3c, #c0392b);
    color: white;
  }

  .prodotto-content {
    padding: 30px;
    color: white;
  }

  .prodotto-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 16px;
    gap: 16px;
  }

  .prodotto-header h3 {
    font-size: 1.4rem;
    font-weight: 600;
    color: #f1c40f;
    flex: 1;
  }

  .categoria-tag {
    background: rgba(212, 175, 55, 0.2);
    color: #d4af37;
    padding: 4px 12px;
    border-radius: 12px;
    font-size: 0.8rem;
    font-weight: 500;
    white-space: nowrap;
  }

  .descrizione {
    color: #ccc;
    line-height: 1.6;
    margin-bottom: 20px;
  }

  .prezzi {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 20px;
    flex-wrap: wrap;
  }

  .prezzo-originale {
    color: #999;
    text-decoration: line-through;
    font-size: 1.1rem;
  }

  .prezzo-scontato {
    color: #f1c40f;
    font-size: 1.5rem;
    font-weight: 700;
  }

  .risparmio {
    background: linear-gradient(45deg, #27ae60, #2ecc71);
    color: white;
    padding: 4px 12px;
    border-radius: 12px;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .taglie {
    margin-bottom: 24px;
  }

  .taglie-label {
    color: #ccc;
    font-size: 0.9rem;
    display: block;
    margin-bottom: 8px;
  }

  .taglie-list {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .taglia {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    padding: 6px 12px;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 500;
  }

  .prodotto-actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .btn-acquista {
    background: linear-gradient(45deg, #d4af37, #f1c40f);
    color: #1a1a1a;
    padding: 12px 24px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    transition: all 0.3s ease;
    flex: 1;
    justify-content: center;
  }

  .btn-acquista:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(212, 175, 55, 0.4);
  }

  .btn-info {
    background: transparent;
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
    padding: 12px 24px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 500;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    transition: all 0.3s ease;
    justify-content: center;
  }

  .btn-info:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: #d4af37;
  }

  /* CTA Saldi */
  .cta-saldi {
    padding: 80px 0;
    background: linear-gradient(135deg, #d4af37, #f1c40f);
  }

  .cta-content {
    text-align: center;
    color: #1a1a1a;
  }

  .cta-content h2 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 20px;
  }

  .cta-content p {
    font-size: 1.2rem;
    margin-bottom: 40px;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-buttons {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary, .btn-secondary {
    padding: 16px 32px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    min-width: 200px;
    text-align: center;
  }

  .btn-primary {
    background: #1a1a1a;
    color: white;
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 30px rgba(26, 26, 26, 0.3);
  }

  .btn-secondary {
    background: transparent;
    color: #1a1a1a;
    border: 2px solid #1a1a1a;
  }

  .btn-secondary:hover {
    background: #1a1a1a;
    color: white;
    transform: translateY(-3px);
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .hero-text h1 {
      font-size: 2.5rem;
    }

    .hero-stats {
      gap: 40px;
    }

    .stat .number {
      font-size: 2.2rem;
    }

    .categorie-grid,
    .prodotti-grid {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .prodotto-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 12px;
    }

    .prezzi {
      flex-direction: column;
      align-items: flex-start;
      gap: 8px;
    }

    .prodotto-actions {
      flex-direction: column;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }
  }

  @media (max-width: 480px) {
    .container {
      padding: 0 15px;
    }

    .hero-text h1 {
      font-size: 2rem;
    }

    .categorie-saldi,
    .prodotti-saldi {
      padding: 60px 0;
    }

    .prodotto-content {
      padding: 20px;
    }
  }
</style>

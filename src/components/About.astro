---
// About Section Component
---

<section id="chi-siamo" class="about-section">	<div class="container">		<div class="section-header scroll-animate">
			<h2 class="section-title">Chi Siamo</h2>
			<div class="section-divider"></div>
			<p class="section-subtitle">Una storia familiare di passione e dedizione dal 1975</p>
		</div>
		
		<div class="about-content">
			<div class="about-text">
				<div class="about-card scroll-animate" data-delay="0.2">
					<div class="year-badge">1975</div>
					<h3>Le Origini</h3>					<p>
						Tutto inizia nel 1975, quando il padre di Pasquale Martino apre il negozio di abbigliamento 
						a San Giovanni Rotondo. Una piccola attività familiare che nasce dalla passione per 
						l'eleganza e la qualità, destinata a diventare un punto di riferimento per l'abbigliamento 
						nella città.
					</p>
				</div>
				
				<div class="about-card scroll-animate" data-delay="0.4">
					<div class="year-badge">1999</div>
					<h3>Nasce il Brand Kelua</h3>					<p>
						Nel 1999, Pasquale Martino eredita la passione familiare e crea il brand "Kelua", 
						trasformando la tradizionale attività in un marchio riconosciuto per stile e qualità. 
						Una storia di continuità che unisce tradizione e innovazione, mantenendo sempre 
						l'attenzione al cliente al centro di tutto.
					</p>
				</div>
				
				<div class="about-card scroll-animate" data-delay="0.6">
					<div class="year-badge">Oggi</div>
					<h3>La Nostra Missione</h3>
					<p>
						Oggi Kelua continua a essere sinonimo di eleganza e qualità. Due generazioni 
						di esperienza, quasi 50 anni di storia e un futuro sempre orientato all'eccellenza 
						nell'abbigliamento. Ogni capo racconta una storia, proprio come la nostra.
					</p>
				</div>
			</div>
			
			<!-- Sezione Storia Fotografica -->
			<div class="storia-fotografica scroll-animate" data-delay="0.8">
				<h3 class="storia-title">Un Viaggio nel Tempo</h3>
				<p class="storia-subtitle">Dalle origini ai giorni nostri: l'evoluzione di Kelua attraverso le immagini</p>
				
				<div class="foto-storia-grid">
					<div class="foto-storia-item">
						<div class="foto-container">
							<img src="/fotoEsterno1980.png" alt="Negozio Kelua nel 1980" class="foto-storica">
							<div class="foto-overlay">
								<div class="foto-info">
									<span class="foto-anno">1980</span>
									<span class="foto-descrizione">I primi anni</span>
								</div>
							</div>
						</div>
						<div class="foto-caption">
							<h4>Le Origini</h4>
							<p>Il negozio nei primi anni di attività, quando tutto iniziò con la passione di una famiglia per l'abbigliamento di qualità.</p>
						</div>
					</div>
					
					<div class="foto-storia-item">
						<div class="foto-container">
							<img src="/fotoEsternoAttuale.jpg" alt="Negozio Kelua oggi" class="foto-storica">
							<div class="foto-overlay">
								<div class="foto-info">
									<span class="foto-anno">Oggi</span>
									<span class="foto-descrizione">La modernità</span>
								</div>
							</div>
						</div>
						<div class="foto-caption">
							<h4>Il Presente</h4>
							<p>Kelua oggi: un punto di riferimento per l'eleganza a San Giovanni Rotondo, con uno stile rinnovato ma fedele alla tradizione.</p>
						</div>
					</div>
				</div>
			</div>
			
			<!-- Sezione Servizi -->
			<div class="servizi-section scroll-animate" data-delay="1.0">
				<h3 class="servizi-title">I Nostri Servizi</h3>
				<p class="servizi-subtitle">Esperienza, qualità e attenzione al dettaglio per ogni tua esigenza</p>
			</div>
			
					<div class="services-grid">
			<div class="service-item scroll-animate service-card-1" data-delay="0.1">
				<div class="service-image">
					<img src="/abiti-cerimonia-1.png" alt="Abiti da Cerimonia Eleganti" class="service-photo">
					<div class="service-overlay">
						<i class="fas fa-crown service-icon"></i>
					</div>
					<div class="service-badge premium">Premium</div>
				</div>
				<div class="service-content">
					<h4>Abiti da Cerimonia</h4>
					<p>Collezioni eleganti per matrimoni, eventi speciali e cerimonie importanti</p>
					<div class="service-features">
						<span class="feature-tag">✨ Eleganza</span>
						<span class="feature-tag">👑 Luxury</span>
					</div>
				</div>
			</div>
			
			<div class="service-item scroll-animate service-card-2" data-delay="0.2">
				<div class="service-image">
					<img src="/sportswear-casual.png" alt="Abbigliamento Sportswear" class="service-photo">
					<div class="service-overlay">
						<i class="fas fa-running service-icon"></i>
					</div>
					<div class="service-badge sport">Sport</div>
				</div>
				<div class="service-content">
					<h4>Abbigliamento Sportswear</h4>
					<p>Capi casual e sportivi per il tempo libero, comfort senza rinunciare allo stile</p>
					<div class="service-features">
						<span class="feature-tag">🏃 Comfort</span>
						<span class="feature-tag">⚡ Performance</span>
					</div>
				</div>
			</div>
			
			<div class="service-item scroll-animate service-card-3" data-delay="0.3">
				<div class="service-image">
					<img src="/servizio-sartoriale.jpeg" alt="Servizio Sartoriale" class="service-photo">
					<div class="service-overlay">
						<i class="fas fa-scissors service-icon"></i>
					</div>
					<div class="service-badge craft">Artigianale</div>
				</div>
				<div class="service-content">
					<h4>Servizio Sartoriale</h4>
					<p>Personalizzazioni e aggiustamenti per un fit perfetto su ogni capo</p>
					<div class="service-features">
						<span class="feature-tag">✂️ Su Misura</span>
						<span class="feature-tag">🎯 Precisione</span>
					</div>
				</div>
			</div>
			
			<div class="service-item scroll-animate service-card-4" data-delay="0.4">
				<div class="service-image">
					<img src="/abiti-cerimonia-2.png" alt="Consulenza Personalizzata" class="service-photo">
					<div class="service-overlay">
						<i class="fas fa-star service-icon"></i>
					</div>
					<div class="service-badge exclusive">Esclusiva</div>
				</div>
				<div class="service-content">
					<h4>Consulenza Personalizzata</h4>
					<p>I nostri esperti ti aiutano a trovare il look perfetto per ogni occasione</p>
					<div class="service-features">
						<span class="feature-tag">👨‍💼 Expert</span>
						<span class="feature-tag">🎨 Style</span>
					</div>
				</div>
			</div>
		</div>
		</div>
	</div>
</section>

<style>
	.about-section {
		padding: 100px 0;
		background: var(--dark-gradient);
		position: relative;
		scroll-margin-top: 120px;
	}

	.about-section::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: radial-gradient(ellipse at top, rgba(212, 175, 55, 0.1) 0%, transparent 70%);
		pointer-events: none;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
		position: relative;
		z-index: 2;
	}

	.about-section .section-title {
		font-family: 'Glida Display Regular', 'Playfair Display', serif;
		font-size: 4rem;
		font-weight: 300;
		background: linear-gradient(135deg, #ffd700 0%, #d4af37 40%, #f1c40f 60%, #ffeb3b 100%);
		background-size: 300% 300%;
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		text-shadow: 0 0 30px rgba(255, 215, 0, 0.4);
		letter-spacing: 3px;
		margin-bottom: 30px;
		position: relative;
		display: inline-block;
		animation: shimmer-title 4s ease-in-out infinite, float-title 6s ease-in-out infinite;
		filter: drop-shadow(0 4px 15px rgba(212, 175, 55, 0.5));
		transition: all 0.3s ease;
		cursor: default;
	}
	
	.about-section .section-title:hover {
		filter: drop-shadow(0 8px 25px rgba(212, 175, 55, 0.8));
		transform: scale(1.02);
		animation-duration: 2s, 3s;
	}
	
	.about-section .section-title::after {
		content: '';
		position: absolute;
		bottom: -15px;
		left: 50%;
		transform: translateX(-50%);
		width: 120px;
		height: 2px;
		background: linear-gradient(90deg, transparent, #ffd700, #d4af37, #ffd700, transparent);
		background-size: 200% 100%;
		box-shadow: 
			0 0 20px rgba(212, 175, 55, 0.7),
			0 0 40px rgba(255, 215, 0, 0.3);
		animation: shimmer-line 3s ease-in-out infinite, pulse-line 2s ease-in-out infinite;
	}
	
	.about-section .section-title::before {
		content: '';
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		width: 100%;
		height: 100%;
		background: radial-gradient(ellipse, rgba(212, 175, 55, 0.1) 0%, transparent 70%);
		z-index: -1;
		border-radius: 50%;
		animation: subtle-glow 3s ease-in-out infinite;
	}
	
	@keyframes subtle-glow {
		0%, 100% { 
			opacity: 0.6; 
			transform: translate(-50%, -50%) scale(1);
		}
		50% { 
			opacity: 1; 
			transform: translate(-50%, -50%) scale(1.1);
		}
	}
	
	@keyframes shimmer-title {
		0% { 
			background-position: 0% 50%;
		}
		50% { 
			background-position: 100% 50%;
		}
		100% { 
			background-position: 0% 50%;
		}
	}
	
	@keyframes float-title {
		0%, 100% { 
			transform: translateY(0px);
		}
		50% { 
			transform: translateY(-3px);
		}
	}
	
	@keyframes shimmer-line {
		0% { 
			background-position: -200% 0%;
		}
		100% { 
			background-position: 200% 0%;
		}
	}
	
	@keyframes pulse-line {
		0%, 100% { 
			opacity: 0.8;
			transform: translateX(-50%) scaleX(1);
		}
		50% { 
			opacity: 1;
			transform: translateX(-50%) scaleX(1.05);
		}
	}

	.about-section .section-subtitle {
		color: rgba(255, 255, 255, 0.7);
	}

	.about-content {
		display: grid;
		grid-template-columns: 1fr;
		gap: 4rem;
		position: relative;
		z-index: 2;
	}

	.about-text {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		margin-bottom: 2rem;
	}

	.about-card {
		background: rgba(0, 0, 0, 0.4);
		padding: 2.5rem;
		border-radius: 20px;
		text-align: center;
		transition: all 0.4s ease;
		border: 1px solid rgba(212, 175, 55, 0.2);
		backdrop-filter: blur(15px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
	}

	.about-card:hover {
		transform: translateY(-10px);
		border-color: var(--accent-color);
		box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
	}
	.year-badge {
		background: var(--gold-gradient);
		color: var(--secondary-color);
		font-size: 2rem;
		font-weight: 700;
		padding: 1rem 2rem;
		border-radius: 50px;
		display: inline-block;
		margin-bottom: 1.5rem;
		box-shadow: 0 8px 25px rgba(212, 175, 55, 0.4);
		transition: all 0.3s ease;
	}

	/* Stile speciale per il badge "Oggi" */
	.about-card:last-child .year-badge {
		background: linear-gradient(135deg, #CFB53B, #d4af37);
		animation: pulse 2s infinite;
		position: relative;
		overflow: hidden;
	}

	.about-card:last-child .year-badge::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
		animation: shimmer 3s infinite;
	}

	@keyframes pulse {
		0%, 100% { transform: scale(1); }
		50% { transform: scale(1.05); }
	}

	@keyframes shimmer {
		0% { left: -100%; }
		100% { left: 100%; }
	}

	.about-card i {
		font-size: 3rem;
		color: var(--accent-light);
		margin-bottom: 1.5rem;
		text-shadow: 0 4px 8px rgba(212, 175, 55, 0.3);
	}

	.about-card h3 {
		font-family: 'Playfair Display', serif;
		color: var(--accent-light);
		font-size: 1.8rem;
		margin-bottom: 1.5rem;
		font-weight: 600;
	}

	.about-card p {
		color: rgba(255, 255, 255, 0.8);
		line-height: 1.8;
		font-size: 1.1rem;
	}

	/* Storia Fotografica */
	.storia-fotografica {
		margin: 4rem 0;
		text-align: center;
	}

	.storia-title {
		font-family: 'Playfair Display', serif;
		color: var(--accent-light);
		font-size: 2.5rem;
		margin-bottom: 1rem;
		font-weight: 600;
		text-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
	}

	.storia-subtitle {
		color: rgba(255, 255, 255, 0.7);
		font-size: 1.2rem;
		margin-bottom: 3rem;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
		line-height: 1.6;
	}

	/* Sezione Servizi */
	.servizi-section {
		margin: 4rem 0 3rem 0;
		text-align: center;
	}

	.servizi-title {
		font-family: 'Playfair Display', serif;
		color: var(--accent-light);
		font-size: 2.5rem;
		margin-bottom: 1rem;
		font-weight: 600;
		text-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
	}

	.servizi-subtitle {
		color: rgba(255, 255, 255, 0.7);
		font-size: 1.2rem;
		margin-bottom: 0;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
		line-height: 1.6;
	}

	.foto-storia-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
		gap: 3rem;
		margin-top: 3rem;
	}

	.foto-storia-item {
		background: rgba(0, 0, 0, 0.4);
		border-radius: 20px;
		overflow: hidden;
		transition: all 0.4s ease;
		border: 1px solid rgba(212, 175, 55, 0.2);
		backdrop-filter: blur(15px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
	}

	.foto-storia-item:hover {
		transform: translateY(-10px);
		border-color: var(--accent-color);
		box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
	}

	.foto-container {
		position: relative;
		height: 300px;
		overflow: hidden;
	}

	.foto-storica {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: transform 0.5s ease;
		filter: sepia(20%) contrast(1.1);
	}

	.foto-storia-item:hover .foto-storica {
		transform: scale(1.1);
		filter: sepia(0%) contrast(1.2);
	}

	.foto-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			45deg, 
			rgba(0, 0, 0, 0.7) 0%, 
			rgba(212, 175, 55, 0.2) 50%, 
			rgba(0, 0, 0, 0.7) 100%
		);
		display: flex;
		align-items: center;
		justify-content: center;
		opacity: 0;
		transition: all 0.4s ease;
	}

	.foto-storia-item:hover .foto-overlay {
		opacity: 1;
	}

	.foto-info {
		text-align: center;
		transform: translateY(20px);
		transition: transform 0.4s ease;
	}

	.foto-storia-item:hover .foto-info {
		transform: translateY(0);
	}

	.foto-anno {
		display: block;
		font-size: 3rem;
		font-weight: 700;
		color: var(--accent-light);
		text-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
		margin-bottom: 0.5rem;
	}

	.foto-descrizione {
		display: block;
		font-size: 1.1rem;
		color: rgba(255, 255, 255, 0.9);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 2px;
	}

	.foto-caption {
		padding: 2rem;
		text-align: center;
	}

	.foto-caption h4 {
		font-family: 'Playfair Display', serif;
		color: var(--accent-light);
		font-size: 1.5rem;
		margin-bottom: 1rem;
		font-weight: 600;
	}

	.foto-caption p {
		color: rgba(255, 255, 255, 0.8);
		line-height: 1.6;
		font-size: 1rem;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2.5rem;
		perspective: 1000px;
	}

	.service-item {
		background: rgba(0, 0, 0, 0.4);
		border-radius: 25px;
		overflow: hidden;
		transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
		border: 1px solid rgba(212, 175, 55, 0.2);
		backdrop-filter: blur(20px);
		box-shadow: 
			0 10px 40px rgba(0, 0, 0, 0.3),
			0 0 0 1px rgba(212, 175, 55, 0.1);
		position: relative;
		transform: translateY(50px);
		opacity: 0;
		will-change: transform, opacity;
	}

	/* Staggered Entry Animation */
	.service-item.scroll-animate.visible {
		transform: translateY(0);
		opacity: 1;
		animation: slideInUp 0.8s ease-out both;
	}

	.service-card-1.visible { animation-delay: 0.1s; }
	.service-card-2.visible { animation-delay: 0.2s; }
	.service-card-3.visible { animation-delay: 0.3s; }
	.service-card-4.visible { animation-delay: 0.4s; }

	@keyframes slideInUp {
		0% {
			transform: translateY(60px) rotateX(15deg);
			opacity: 0;
		}
		50% {
			transform: translateY(-10px) rotateX(0deg);
			opacity: 0.8;
		}
		100% {
			transform: translateY(0) rotateX(0deg);
			opacity: 1;
		}
	}

	/* Advanced Hover Effects */
	.service-item:hover {
		transform: translateY(-15px) rotateX(5deg) rotateY(2deg);
		box-shadow: 
			0 25px 60px rgba(0, 0, 0, 0.5),
			0 0 0 1px rgba(212, 175, 55, 0.4),
			0 0 30px rgba(212, 175, 55, 0.2);
		border-color: var(--accent-color);
	}

	/* Parallax Effect on Hover */
	.service-item:hover .service-image {
		transform: scale(1.05) translateZ(20px);
	}

	.service-item:hover .service-content {
		transform: translateZ(10px);
	}

	.service-image {
		width: 100%;
		height: 240px;
		position: relative;
		overflow: hidden;
		transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
	}

	.service-photo {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
		filter: brightness(0.9) contrast(1.1);
	}

	/* Service Badge Styles */
	.service-badge {
		position: absolute;
		top: 15px;
		right: 15px;
		background: linear-gradient(135deg, rgba(212, 175, 55, 0.9), rgba(212, 175, 55, 0.7));
		color: var(--secondary-color);
		padding: 6px 14px;
		border-radius: 20px;
		font-size: 0.8rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 1px;
		backdrop-filter: blur(10px);
		box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
		z-index: 2;
		transform: translateY(-10px);
		opacity: 0;
		animation: slideInBadge 0.6s ease-out 0.8s both;
	}

	@keyframes slideInBadge {
		to {
			transform: translateY(0);
			opacity: 1;
		}
	}

	/* Badge Color Variations */
	.service-badge.premium {
		background: linear-gradient(135deg, #9b59b6, #8e44ad);
		box-shadow: 0 4px 15px rgba(155, 89, 182, 0.3);
	}

	.service-badge.sport {
		background: linear-gradient(135deg, #3498db, #2980b9);
		box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
	}

	.service-badge.craft {
		background: linear-gradient(135deg, #e67e22, #d35400);
		box-shadow: 0 4px 15px rgba(230, 126, 34, 0.3);
	}

	.service-badge.exclusive {
		background: linear-gradient(135deg, #e74c3c, #c0392b);
		box-shadow: 0 4px 15px rgba(231, 76, 60, 0.3);
	}

	.service-overlay {
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%) scale(0.8);
		background: linear-gradient(135deg, rgba(0, 0, 0, 0.8), rgba(212, 175, 55, 0.2));
		border-radius: 50%;
		width: 80px;
		height: 80px;
		display: flex;
		align-items: center;
		justify-content: center;
		opacity: 0;
		transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
		border: 2px solid rgba(212, 175, 55, 0.3);
	}

	.service-icon {
		color: var(--accent-light);
		font-size: 2rem;
		transition: all 0.4s ease;
		text-shadow: 0 2px 8px rgba(212, 175, 55, 0.5);
	}

	/* Advanced Hover Animations */
	.service-item:hover .service-photo {
		transform: scale(1.15);
		filter: brightness(1.1) contrast(1.2);
	}

	.service-item:hover .service-overlay {
		opacity: 1;
		transform: translate(-50%, -50%) scale(1) rotate(360deg);
	}

	.service-item:hover .service-icon {
		transform: scale(1.2);
		animation: iconPulse 1s infinite alternate;
	}

	@keyframes iconPulse {
		0% { transform: scale(1.2); filter: drop-shadow(0 0 5px rgba(212, 175, 55, 0.5)); }
		100% { transform: scale(1.4); filter: drop-shadow(0 0 15px rgba(212, 175, 55, 0.8)); }
	}

	/* Service Content Area */
	.service-content {
		padding: 2rem;
		transition: all 0.6s ease;
		transform: translateY(0);
	}

	.service-item h4 {
		font-family: 'Playfair Display', serif;
		color: var(--accent-light);
		font-size: 1.4rem;
		margin: 0 0 1rem 0;
		font-weight: 600;
		text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
		transition: all 0.3s ease;
	}

	.service-item p {
		color: rgba(255, 255, 255, 0.85);
		line-height: 1.7;
		font-size: 1rem;
		margin-bottom: 1.5rem;
		transition: all 0.3s ease;
	}

	/* Feature Tags */
	.service-features {
		display: flex;
		gap: 0.8rem;
		flex-wrap: wrap;
		margin-top: 1rem;
		opacity: 0;
		transform: translateY(20px);
		animation: fadeInFeatures 0.6s ease-out 1.2s both;
	}

	@keyframes fadeInFeatures {
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.feature-tag {
		background: linear-gradient(135deg, rgba(212, 175, 55, 0.2), rgba(212, 175, 55, 0.1));
		color: rgba(255, 255, 255, 0.9);
		padding: 6px 12px;
		border-radius: 15px;
		font-size: 0.85rem;
		font-weight: 500;
		border: 1px solid rgba(212, 175, 55, 0.3);
		backdrop-filter: blur(5px);
		transition: all 0.3s ease;
		cursor: default;
	}

	.feature-tag:hover {
		background: linear-gradient(135deg, rgba(212, 175, 55, 0.4), rgba(212, 175, 55, 0.2));
		border-color: rgba(212, 175, 55, 0.6);
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
	}

	/* Enhanced Hover Effects for Content */
	.service-item:hover h4 {
		color: #ffffff;
		transform: translateX(5px);
	}

	.service-item:hover p {
		color: rgba(255, 255, 255, 0.95);
		transform: translateX(3px);
	}

	.service-item:hover .feature-tag {
		transform: translateY(-3px);
		box-shadow: 0 6px 15px rgba(212, 175, 55, 0.4);
	}

	/* Ripple Effect */
	.service-ripple {
		position: absolute;
		top: 50%;
		left: 50%;
		width: 0;
		height: 0;
		border-radius: 50%;
		background: radial-gradient(circle, rgba(212, 175, 55, 0.3) 0%, transparent 70%);
		animation: rippleEffect 0.6s ease-out;
		pointer-events: none;
		z-index: 1;
	}

	@keyframes rippleEffect {
		0% {
			width: 0;
			height: 0;
			opacity: 1;
		}
		100% {
			width: 300px;
			height: 300px;
			margin: -150px 0 0 -150px;
			opacity: 0;
		}
	}

	/* Floating Animation for Cards */
	.service-item.visible {
		animation: cardFloat 6s ease-in-out infinite;
	}

	.service-card-1.visible { animation-delay: 0s; }
	.service-card-2.visible { animation-delay: 1.5s; }
	.service-card-3.visible { animation-delay: 3s; }
	.service-card-4.visible { animation-delay: 4.5s; }

	@keyframes cardFloat {
		0%, 100% { transform: translateY(0px); }
		50% { transform: translateY(-8px); }
	}

	@media (max-width: 768px) {
		.about-section {
			padding: 80px 0;
		}
		
		.about-section .section-title {
			font-size: 3rem;
			letter-spacing: 2px;
		}
		
		.about-section .section-title::after {
			width: 100px;
		}

		.about-text {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.services-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
			perspective: none;
		}

		.about-card {
			padding: 2rem;
		}

		.service-image {
			height: 200px;
		}

		.service-content {
			padding: 1.5rem;
		}

		/* Disable complex 3D transforms on mobile */
		.service-item:hover {
			transform: translateY(-5px);
			box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
		}
		
		/* Reduce title animations on mobile */
		.about-section .section-title {
			animation-duration: 6s, 8s;
		}

		/* Simplified animations for mobile */
		.service-item.visible {
			animation: slideInUpMobile 0.6s ease-out both;
		}

		@keyframes slideInUpMobile {
			0% {
				transform: translateY(30px);
				opacity: 0;
			}
			100% {
				transform: translateY(0);
				opacity: 1;
			}
		}

		/* Reduce floating animation on mobile */
		@keyframes cardFloat {
			0%, 100% { transform: translateY(0px); }
			50% { transform: translateY(-4px); }
		}

		.feature-tag {
			font-size: 0.8rem;
			padding: 5px 10px;
		}

		/* Storia Fotografica - Mobile */
		.storia-fotografica {
			margin: 3rem 0;
		}

		.storia-title {
			font-size: 2rem;
		}

		.storia-subtitle {
			font-size: 1.1rem;
			margin-bottom: 2rem;
		}

		/* Sezione Servizi - Mobile */
		.servizi-section {
			margin: 3rem 0 2rem 0;
		}

		.servizi-title {
			font-size: 2rem;
		}

		.servizi-subtitle {
			font-size: 1.1rem;
		}

		.foto-storia-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.foto-container {
			height: 250px;
		}

		.foto-caption {
			padding: 1.5rem;
		}

		.foto-anno {
			font-size: 2.5rem;
		}

		.foto-descrizione {
			font-size: 1rem;
		}
	}

	@media (max-width: 480px) {
		.about-section .section-title {
			font-size: 2.5rem;
			letter-spacing: 1px;
		}
		
		.about-section .section-title::after {
			width: 80px;
		}
		
		.about-card {
			padding: 1.5rem;
		}

		.year-badge {
			font-size: 1.5rem;
			padding: 0.8rem 1.5rem;
		}

		.about-card h3 {
			font-size: 1.5rem;
		}

		.about-card i {
			font-size: 2.5rem;
		}

		/* Storia Fotografica - Piccoli schermi */
		.storia-title {
			font-size: 1.8rem;
		}

		.storia-subtitle {
			font-size: 1rem;
			margin-bottom: 1.5rem;
		}

		/* Sezione Servizi - Piccoli schermi */
		.servizi-title {
			font-size: 1.8rem;
		}

		.servizi-subtitle {
			font-size: 1rem;
		}

		.foto-storia-grid {
			gap: 1.5rem;
		}

		.foto-container {
			height: 220px;
		}

		.foto-caption {
			padding: 1rem;
		}

		.foto-caption h4 {
			font-size: 1.3rem;
		}

		.foto-caption p {
			font-size: 0.9rem;
		}

		.foto-anno {
			font-size: 2rem;
		}

		.foto-descrizione {
			font-size: 0.9rem;
			letter-spacing: 1px;
		}
	}
	
	/* Accessibility: Reduce motion for users who prefer it */
	@media (prefers-reduced-motion: reduce) {
		.about-section .section-title {
			animation: none;
		}
		
		.about-section .section-title::after {
			animation: none;
		}
		
		.about-section .section-title::before {
			animation: none;
		}
		
		.about-section .section-title:hover {
			transform: none;
		}
	}
</style>

<script>
	// Service Cards Animation Observer
	function initServiceAnimations() {
		const serviceCards = document.querySelectorAll('.service-item.scroll-animate');
		
		const observer = new IntersectionObserver((entries) => {
			entries.forEach((entry) => {
				if (entry.isIntersecting) {
					// Add visible class with a small delay for smoother effect
					setTimeout(() => {
						entry.target.classList.add('visible');
					}, 100);
					observer.unobserve(entry.target);
				}
			});
		}, {
			threshold: 0.2,
			rootMargin: '50px 0px -50px 0px'
		});

		serviceCards.forEach((card) => {
			observer.observe(card);
		});
	}

	// Enhanced hover effects for service cards
	function initServiceHoverEffects() {
		const serviceItems = document.querySelectorAll('.service-item');
		
		serviceItems.forEach((item) => {
			item.addEventListener('mouseenter', () => {
				// Add ripple effect
				const ripple = document.createElement('div');
				ripple.className = 'service-ripple';
				item.appendChild(ripple);
				
				// Remove ripple after animation
				setTimeout(() => {
					if (ripple.parentNode) {
						ripple.parentNode.removeChild(ripple);
					}
				}, 600);
			});
		});
	}

	// Initialize when DOM is loaded
	document.addEventListener('DOMContentLoaded', () => {
		initServiceAnimations();
		initServiceHoverEffects();
	});

	// Reinitialize on Astro page navigation
	document.addEventListener('astro:page-load', () => {
		initServiceAnimations();
		initServiceHoverEffects();
	});
</script>
